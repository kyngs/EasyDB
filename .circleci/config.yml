version: 2.1

jobs:
  maven-build:
    docker:
      - image: circleci/openjdk:8-jdk-node
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
workflows:
  maven:
    jobs:
      - maven-build